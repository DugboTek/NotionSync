This repository includes a Notion Integration CLI and sync system.

- For full agent guidance and conventions, see `.agents/AGENTS.md` (applies to this repo).
- For Claude-specific usage and the system prompt, see `.claude/`.

