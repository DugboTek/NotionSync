# Claude Usage Guide for Notion Integration

This folder provides Claude-based agents with a single Markdown-based Skill for the local Notion CLI.

Files:
- `skills/notion-cli/SKILL.md` (YAML frontmatter + minimal instructions)

How agents should use this
- Use the `notion-cli` skill to decide and run CLI commands.
